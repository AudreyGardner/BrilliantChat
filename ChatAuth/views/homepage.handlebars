{{#each users as |user| }}
<div class="row align-center mb-5">
  <div class="col-md-5">
    <h2>{{user.username}}<br>{{{get_emoji}}}{{user.zodiac}}</h2>
    <h3><a href="mailto:{{user.email}}">{{user.email}}</a></h3>
  </div>
</div>
{{/each}}
